# Collector quickstart

1. Start the API.
2. Call `/evidence/collect` with a market question.
3. Review the returned evidence package.
4. Call `/forecast/auto` when `ANTHROPIC_API_KEY` is configured.
