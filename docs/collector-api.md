# Collector API

`/evidence/collect` returns the evidence package and readiness blockers. `/forecast/auto` returns both the evidence collection result and the forecast if the package is strong enough.
